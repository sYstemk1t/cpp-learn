#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>


int main()
{
	int x, y, z;
	scanf("%d%d%d", &x, &y, &z);
	//x > y ? (y>z ? printf("最小数z = %d\n", z) : printf("最小数y=%d\n", y)) : (x > z ? printf("最小数z = %d\n", z) : printf("最小数y = %d\n", y)) : (x > z ? rintf("最小数z = %d\n", z) : )
	return 0;
}